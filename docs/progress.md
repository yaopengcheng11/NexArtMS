# 视频三维复现：执行进度

更新日期：2026-09-24

## 一、当前结论

**当前版本 MP4 已完成。新的五阶段流程已经开始实施，M0 技术探针和 M1 场景阶段仍在进行中。G1 场景验收尚未通过，尚未进入正式角色确认或逐镜动作还原。**

新工程与此前 Three.js V2 分开保存。此次导出的 MP4 保留 V2 当前画面与动作，因此也保留该版已有的动作抖动；它不是新骨架和去抖处理后的成果。

| 工作项 | 当前状态 | 本次实际完成内容 |
|---|---|---|
| 当前 V2 视频导出 | 已完成 | 720P MP4、完整 1,203 帧、24 fps，配原片音频 |
| M0 基线与技术探针 | 进行中 | 输入与镜头基线、22 节点刚性角色、三档显示、局部导出往返试验 |
| M1 项目框架与场景阶段 | 进行中 | 统一空场景候选、47 个镜头的透视相机初值，以及场景查看页面 |
| G1 场景确认 | 未通过 | 主要地标与机位误差尚未完成测量，用户尚未确认场景 |
| 正式角色与 T Pose 确认 | 未开始 | 当前小人属于技术试验，不能替代原片角色对应和用户确认 |
| 原视频动作去抖 | 未开始 | 尚未将原视频动作转换为固定骨架连续动画，也未执行全片去抖与接触优化 |
| 完整导出交付 | 未完成 | 仅完成合成角色夹具的部分本地格式试验，不能视作 G5 验收通过 |

## 二、已完成的 MP4

文件：[JWM_ThreeJS_V2_720P.mp4](../public/demo/v2.mp4)

- 画面：1280 × 720，24 fps，1,203 帧，50.125 秒。
- 内容：当前 V2 的完整三维复现画面；47 个镜头。
- 声音：从原片起点同步使用原音频。
- 导出记录未发现错误；音频对齐检查检测到的时间偏移为 0 毫秒。
- AAC 解码后的采样长度约 50.134 秒，包含编码尾部填充；容器及视频时长为 50.125 秒，检测到的音频对齐偏移为 0 毫秒。

记录依据：`reports/baseline.json`。该 MP4 是保存现有成果的交付，不代表后续五阶段流程已经完成。

## 三、M0：刚性角色与三档显示

已建立一个具有 **22 个骨骼／控制节点、21 条受约束父子连接**的刚性人形技术样例，包含整体根节点、躯干、头部、双臂和双腿。

| 外观档位 | 可见部件 | 当前显示规则 |
|---|---:|---|
| CL0 | 2 | 头部和整体躯干 |
| CL1 | 6 | 头、躯干、四条固定长度的整段肢体 |
| CL2 | 27 | 分段四肢、关节及手脚末梢 |

三个档位共用身份、比例、骨架和姿态。切换外观不移动角色根节点，不重置动作。CL1 保留整段肢体长度，不以伸缩手臂或腿部模拟屈肘、屈膝；精确接触仍需用 CL2 检查。

### 已执行的检查

- 8 项局部检查全部通过：层级、固定骨长、肩肘父子传递、三档切换、CL1 固定长度、导出权重、姿态等价以及输入校验。
- 120 个合成姿态采样中，最大相对骨长误差约 `2.22 × 10⁻¹⁶`，低于拟定的 `0.0001` 门槛。
- 三档导出表示均包含真实的 22 骨骨架和绑定初始姿态。
- 每个顶点仅绑定一根骨骼，权重为 100%；无需人工刷混合权重。
- 导出表示与刚性部件在当前姿态下的最大顶点坐标差约 `6.34 × 10⁻⁸` 米。
- 浏览器检查记录了三档的 2／6／27 个部件以及共同的 22 节点骨架。

**这些结果证明的是绑定和数据表示的可行性。当前样例的身高与比例尚未作为原片人物资产确认，尚未生成并确认正式角色 T Pose 参考图，也没有因此通过 G2。**

记录依据：`reports/rig-m0.json`、`reports/browser-m0.json`。

## 四、M0：Blender 本地格式试验

使用由同一角色实现输出的 `rig.glb`，在 Blender 5.2.1 LTS 中执行独立后台空场景试验，没有打开或修改用户已有场景。

| 试验路径 | 实际验证环境 | 结果 |
|---|---|---|
| GLB 导入 | Blender | 通过本合成样例的骨架、权重和动作检查 |
| 保存 `.blend` 后重新打开 | 另一个 Blender 进程 | 通过相同检查 |
| FBX 导出后重新导入 | 另一个 Blender 进程 | 通过相同检查，需显式设置导入时间偏移 |
| USD 导出后重新导入 | 另一个 Blender 进程 | 通过相同检查，含原生 Skeleton／SkelAnimation |

试验对象包含 18,264 个角色顶点，22 骨语义层级保持一致，所有顶点均保持单骨 100% 权重。0–47 帧、24 fps 的单肘合成动画可以播放。单独旋转左肘时，对应前臂和手的 3,312 个顶点跟随，其他部位保持不动，恢复姿态检查通过。

### 已发现并处理的差异

Blender 的 FBX 默认导入会增加 1 帧偏移，将 0–47 帧变为 1–48 帧。探针明确设置 `anim_offset=0.0` 后重新检查，恢复了项目的 0–47 帧时间范围。原始默认偏移结果单独保存，后续正式适配器需要保留此处理。

USD 文件明确记录 Z 轴向上、每单位 1 米、每秒 24 个时间码；它与 Three.js 的 Y 轴向上约定之间由交换步骤转换，不能忽略坐标系元数据。

### 验证边界

- **Maya 和 Houdini 尚未实际导入验证。** Blender 中 FBX／USD 回读成功不能替代这两项。
- **相机、焦距和相机动画交换尚未验证。**
- 动画仅为单肘合成样例，没有承载原视频完整动作。
- 格式间比较使用了三个动作采样点，没有证明所有动画帧数值完全等价。
- 尚未完成正式场景、角色、镜头、音频和清单的整包交付验收。

记录依据：`reports/dcc-summary.md`、`reports/dcc-summary.json` 及对应的 `dcc-*.json`。较早的 `rig-m0.json` 只覆盖角色内存结构检查，其“未验证 GLB 导入”条目属于该局部检查范围；后续 DCC 报告补充了上表中的实际验证，不应扩大解释为所有目标软件均通过。

## 五、M1：统一空场景与逐镜查看

目前已建立一个**待确认的固定场景候选**，主要包含地形、同一组主墓、围挡和稀疏树。47 个镜头引用同一场景；切换镜头时不再按镜头重建或拉伸背景。

- 三维场景中没有角色。
- 已提供 47 个透视机位初值，用于逐镜查看；这些机位仍是估计值。
- 浏览器已检查全部 47 个镜头：切镜时场景变换保持一致、角色数量为 0、画布为 1280 × 720。
- 已从原片精确抽取 141 张开始／中间／结束参考帧。浏览器检查了 50 次帧图显示和原片视频载入，无脚本异常。
- 已通过浏览器实际保存地物位置和意见、重新打开恢复内容的检查；测试内容已恢复，交付包不携带测试历史。
- 8 项骨架检查与 5 项场景／数据检查共 13 项通过；类型检查与网页构建通过。
- 场景尺度以约 1.75 米人物身高作为暂定参照，属于尺度假设，不能宣称真实尺寸已测得。
- 主要背景结构有跨镜画面依据；墓墙背面、山坡纵深等不可见信息仍标为推断。

### 为什么 G1 仍未通过

相机标定、共同地标的重投影误差测量、全部镜头的背景构图核对，以及用户场景确认尚未完成。当前确认保护检查会阻止在机位参照点测量缺失时将 G1 标记为通过。

**可以查看和提出场景意见，不代表场景已达到正式验收标准。**

记录依据：`reports/scene-evidence.md`、`reports/browser-m0.json`、`reports/browser-scene.json`。

### 核对入口补充

已补充“用户核对指引”，区分用户的目视审查与制作方的机位标定。可一键查看整体、俯视及 S11／S21／S33 三个代表镜头，并带镜号记录意见。当前每镜只有静态初值，首／中／尾按钮只切换原片参考；像素误差测量仍未完成；正式确认的保存入口已接通，但仍受测量条件约束。

### 可点击的流程演练

按用户“先跑一遍、假设没有意见”的要求增加独立演练入口。首页底部“演练通过 → 角色确认”可直接进入第二步，之后依次通过关键姿态、完整镜头和导出。也可从第一步开始，刷新恢复，并回看已访问步骤。

阶段 2 使用三档合成小人和原片身份参考；阶段 3／4 使用旧版 V2 关键帧和视频；阶段 5 下载现有视频及技术样例。正式 T Pose 图片、新流程关键姿态、去抖动作和全片三维整包仍待制作。演练进度独立保存，`demo_passed` 不会变更真实验收状态。

正式场景确认已接通保存：通过时生成新项目版本，记录确认场景版本和时间，保留此前历史。当前标定未完成，正式按钮仍不开放。

本次流程核验：19 项自动检查通过；在独立测试数据上从首页点击通过，走完五阶段，检查刷新恢复、视频播放与 GLB 下载，正式项目数据保持不变。证据：`reports/rehearsal-browser.json`。实际交付页面的按钮与新接口已复核：`reports/rehearsal-live.json`。

### 完整镜头的同步对照

第四步“连起来看一遍”已接入原片和 V2 三维视频的统一播放器，支持并排对照、透明叠加、三维单看、47 镜时间带、逐帧、按秒定位、切镜、原声和速度控制。叠加模式可调原片比例和三分参考线，切换显示方式保留播放位置。

两路媒体均为 24 fps、1,203 帧、50.125 秒。已修复小数时间边界导致浏览器显示前一帧的问题：界面仍显示名义帧时间，解码定位进入目标帧内部 0.1 毫秒。

本次实际核验：

- 26 项自动检查通过，其中 7 项覆盖同步控制器；类型检查和生产构建通过。
- 浏览器定位第 98、99、662、1184、1202 帧（数据从 0 开始），通过实际解码帧时间确认两路均显示目标帧。
- 并排 1×、叠加 2×、三维单看 0.5×，分别连续采样约 2.5 秒；采样中两路播放时钟最大偏差分别为 0.002、0.290、0.006 帧以内。这是本机局部播放测量，不能推断所有设备都逐帧零延迟。
- 快速定位、最后一帧重播、键盘切镜／逐帧、叠加画幅重合和 35% 比例、单路音频、390 像素宽页面无横向溢出，以及媒体失败后两路暂停，均通过浏览器检查。
- 已目视检查桌面并排、叠加和手机截图。检查前后演练进度保持一致。
- 更新当前 8200 页面后，以叠加模式 2× 速度完整播放全部 47 个镜头，838 次采样中播放时钟最大偏差约 0.338 帧；两路正常停在最后一帧，无脚本异常，演练进度未改变。该时钟测量不等同于所有播放瞬间的显示帧完全相同。

证据：`reports/comparison-browser.json`、`reports/comparison-live.json`、`reports/comparison-side.png`、`reports/comparison-overlay.png`、`reports/comparison-mobile.png`。

当前仍播放此前导出的 V2 成果；本次增加对照工具，不代表新动作去抖或场景标定已完成。

### 对照页面风格统一

已将播放器统一到项目的浅米白、灰绿和深绿配色，沿用卡片边框、圆角、标题层级、主次按钮及分段切换样式。橙蓝时间带继续表示角色身份。已检查桌面全页、叠加和 390 像素宽布局，以及切换显示方式、透明度画幅、定位和播放／暂停；没有改动同步控制器或演练记录。证据：`reports/comparison-style.json` 与 `reports/comparison-style-*.png`。

## 六、下一步与进入条件

1. 继续完成统一场景及各镜头机位的原片对照，优先检查同时出现墓墙、围挡和树等共同地标的代表镜头。
2. 补齐测量与问题记录，修正统一场地和相机参数，形成可供用户确认的 G1 候选。
3. G1 通过后，再制作正式角色对应与三档 T Pose 对照，并执行 G2。
4. 关键姿态经过确认后，进入逐镜连续动作求解，重点处理固定骨长、异常检测、时间连续性和支撑接触。
5. 后续分别完成相机交换、目标软件导入和整包导出验收。

**原视频动作抖动是后续必须解决的问题，当前并未开始该项处理，也没有以这次技术试验替代去抖验收。**
